# Local operator configuration and tunnel credentials must never be committed.
local/
*.local.yml
*.local.yaml
*.pid
*.token
*.json.secret
cloudflared/*.json
cloudflared/config.yml
cloudflared/config.yaml
